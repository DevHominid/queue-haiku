extends default

block content
  h1= haiku.title
  h4 Written by #{haiku.author}
  p= haiku.body
  hr
  a.btn.btn-default(href='/haiku/edit/'+haiku._id) Edit
  a.btn.btn-danger.delete-haiku(href='#',data-id=haiku._id) Delete
